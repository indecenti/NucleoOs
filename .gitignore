
# ANIMA secrets — never commit API keys
tools/anima/.teacher*.json
**/teacher.json
# Gemini key dropped at the repo root for LOCAL testing only — never commit / never ship to SD
GEMINI API KEY*.txt
*API KEY*.txt
*.apikey

# Build artifacts and staging trees (regenerated by tools/)
/build/
/deploy/

# Ad-hoc harness output at the repo root — keep new runs in _scratch/ or $env:TEMP
/*.log
/scratch_*
/tmp_*
/_tmp_*/
/_scratch/
*TEMP*.txt
/devcookie.txt
/*cookie*.txt
/events.ndjson
/bounce_test*.wav
/input.txt
/output.txt

# --- Push GitHub: escludi alberi dati/generati enormi e cloni terzi ---
# Cloni di riferimento terzi (hanno un proprio .git, AGPL — solo riferimento)
/reference/
# Artefatti di build firmware (esp-idf, rigenerati)
/firmware/build/
# Dipendenze Node
node_modules/
# Media generati, cache modelli, dati simulatore sotto tools/
/tools/nfv/out/
/tools/sd-sim/data/
/tools/anima/.cache/
# Blob binari che GitHub rifiuta (>100MB) / artefatti rigenerati
*.gguf
*.npy
*.pcm
