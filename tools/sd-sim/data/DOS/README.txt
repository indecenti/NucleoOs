Drop DOS images here to run them in the DOS Box app.

Recommended format: a .jsdos bundle (a zip prepared for js-dos, e.g. game.jsdos).
Also accepted: .zip, .img, .ima disk images.

The DOS Box app lists this folder, and the emulator runs in your browser
(PC / Android). The device only streams the bytes from the SD card.
